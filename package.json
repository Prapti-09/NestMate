{
  "name": "nestmate-root",
  "version": "1.0.0",
  "description": "Full-stack NestMate Platform",
  "scripts": {
    "start": "concurrently \"cd server && node server.js\" \"cd client && npm run dev\"",
    "install-all": "cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
